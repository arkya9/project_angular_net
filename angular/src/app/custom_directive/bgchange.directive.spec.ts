import { BgchangeDirective } from './bgchange.directive';

describe('BgchangeDirective', () => {
  it('should create an instance', () => {
    const directive = new BgchangeDirective();
    expect(directive).toBeTruthy();
  });
});
